using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace automark.Transformations.Filters
{
    class FilterReverts
    {

// TEMP EXAMPLE.

/*
             if (m_configViewModel == null)
             {
                 GridConfigurationVM viewModel = new GridConfigurationVM(uiContext);
-                GridAspectConfigurationView view = new GridAspectConfigurationView(uiContext);
+                GridAspectConfigurationView view = uiContext == null ? null : new GridAspectConfigurationView(uiContext);
                 viewModel.Init(view, m_aspect);
                 m_configViewModel = viewModel;

             if (m_configViewModel == null)
             {
                 GridConfigurationVM viewModel = new GridConfigurationVM(uiContext);
-                GridAspectConfigurationView view = uiContext == null ? null : new GridAspectConfigurationView(uiContext);
+                GridAspectConfigurationView view = new GridAspectConfigurationView(uiContext);
                 viewModel.Init(view, m_aspect);
                 m_configViewModel = viewModel;
*/ 

    }
}
